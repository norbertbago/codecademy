import numpy as np

store_one = np.array([2, 5, 8, 3, 4, 10, 15, 5])

#average of store_one
mean_store  = np.mean(store_one)

#percentage of number more or equeal than 2
mean_store1 = np.mean(store_one >= 2)
